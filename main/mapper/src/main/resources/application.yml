server:
  port: 8080

spring:
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/db?characterEncoding=utf-8
      username: root
      password: 1717

mybatis:
  mapper-locations: classpath:mappers/*Mapper.xml
  type-aliases-package: com.lzd.entity
